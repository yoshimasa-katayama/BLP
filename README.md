# Replication
* `BLP.R` estimates demand using the nested fixed point algorithm.
* `BLP_supply.R` estimates demand and supply jointly using the nested fixed point algorithm.
* `BLP_merger.R` conducts a counterfactual merger analysis.
