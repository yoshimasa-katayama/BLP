# Replication
* `blp_nfp.R` estimates demand using the nested fixed point algorithm.
* `blp_nfp_supply.R` estimates demand and supply jointly using the nested fixed point algorithm.
* `blp_merger.R` conducts a counterfactual merger analysis.
